<?xml version="1.0" encoding="utf-8"?>
<ModMetadata xmlns:xsd="http://www.w3.org/2001/XMLSchema" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<Name>ScriptedScreens Vector [StationeersLaunchPad]</Name>
	<Author>Gruffuss</Author>
	<Version>0.11.22.0</Version>
	<Description>
[h1]ScriptedScreens Vector[/h1]
[i]Client-side only. Nothing on disk is modified.[/i]

Adds a [b]vector[/b] element type to ScriptedScreens surfaces. Instead of drawing pixel by pixel onto a canvas, you describe a picture once and the game draws it as real geometry — then animates it on your machine, every frame, for free.

[h2]Why you want it[/h2]
A canvas console costs roughly [b]10 FPS[/b], and the cause is the number of drawing operations per frame. Every one is recorded, batched, sent over the network, replayed, and uploaded as a texture — sixty times a second.

This makes that count [b]zero[/b]. The scene is sent once. Movement is described with expressions and evaluated on the client, so an animated console needs no per-frame Lua and no per-frame network traffic.

Tessellation runs on a worker thread. A console with 800 shapes animating at 30 Hz costs about [b]0.4% of a frame[/b] on the main thread — a console being switched on is not measurably different from it being off.

[h2]What you can draw[/h2]
[list]
[*] Rectangles with real rounded corners, ellipses, polygons, SVG paths with holes
[*] Smooth liquid surfaces, waveforms and area charts that read as continuous edges
[*] Catmull-Rom splines, strokes with caps, joins and dashes
[*] Linear, radial and conic gradients, including ones that follow the shape they fill
[*] Real alpha — no more pre-composited opaque palettes
[*] Antialiased edges, sized in screen pixels so they look right at any distance
[*] Clipping to any outline, so a liquid stays in its tank
[*] Pictures from a URL, drawn in scene order
[*] CSS colour filters, gradient masks and matrix transforms on groups
[*] Opacity ramps along a surface, for gas fades that follow a moving edge
[*] Soft drop shadows and inner shadows, evaluated analytically -- no offscreen pass
[*] Text that moves, scales, rotates and scrolls with the artwork, in any font the game has
[*] Scrolling containers for lists longer than the console -- the wheel costs the chip nothing
[*] Clickable nodes, so a row of tiles no longer needs a transparent button over every one
[*] Reusable symbols: write a row or a lamp once, stamp it with different parameters
[/list]

[h2]How it animates[/h2]
Any number can be an expression over [b]t[/b] (time), [b]i[/b] (repeat index) and your own data values:

[code]{ op = "R", x = 10, y = "=100-$fill*100", h = "=$fill*100", f = "#2E8B6E" }[/code]

Send [i]fill[/i] twice a second and the bar moves smoothly at display rate. Values are eased between updates automatically, so a gauge never steps.

[b]hash(i)[/b] gives each instance of a repeat stable pseudo-random values, identical on every client with nothing transmitted — which is what makes a 400-mote particle field cost one node instead of four hundred.

[h2]A scene can be written as text[/h2]
Instead of nested Lua tables, one node per line:

[code]R x=10 y=10 w=44 h=60 rx=8 f=#0B1622
G clip=tank { YS n=24 x==8+i*1.75 y==64-$fill*52 y2=72 f=@liquid }[/code]

A static layout written this way is a string literal, already in the compiled script, so it costs the chip nothing to build. A generated one costs about what the tables cost -- this is a way to write a screen, not a way to buy back your instruction budget.

[h2]Resolution independent[/h2]
Artwork is written in a viewbox and mapped onto whatever size the element is. The same scene fits a 128 px console and a 760 px one, and stays sharp on both.

[h2]Getting started[/h2]
The mod folder contains a numbered set of examples that each run on paste, from a three-shape "hello" up to a complete four-tank gas console, plus a full reference for every node and function.

[h2]Notes[/h2]
[list]
[*] A concave clip draws what it clips once per convex piece, so prefer convex outlines where the design allows
[*] Text updates at the redraw rate rather than instantly, so a label element is still the better choice for something that must change the moment a value does
[*] A broken scene says so: it draws a magenta border and names the fault, rather than looking switched off
[*] Level of detail is automatic -- update rate and curve smoothness both follow on-screen size; consoles off screen or in a paused game cost nothing
[*] Turn on Diagnostics in the settings to see exactly what a scene costs
[/list]

[h2]Changelog[/h2]
[b]0.11.22[/b]
[list]
[*] Text follows a group's skew or uneven scale (m, s), as CSS transforms do; the viewbox fit still leaves letterforms alone.
[*] Radial and conic masks follow their gradient smoothly on any shape, with no specks, and a conic mask keeps a hard edge at its start angle.
[*] Fixed: a click landed anywhere in a node's bounding rectangle -- a circle's corners, a clipped shape's cut-away parts, list rows scrolled out of sight. Clicks now land only on what is drawn.
[*] Fixed: a label covered by the scene's very first shape stayed on top of it.
[*] Fixed: a gradient mask over a group holding only text hid the text entirely.
[*] Fixed: screen captures logged an error per masked label and lost masked text afterwards, and showed images white, text without its gradient or filter colours, and inset text shadows missing.
[*] Inset shadows, masks and colour filters cost less to rebuild.
[*] Examples: 05's path hole and layout, 13 and 14 no longer stretched, 15's clipped label.
[/list]
[b]0.11.21[/b]
[list]
[*] [b]Inset shadows[/b] on shapes (sixth sh field "inset", convex shapes) and on text. Text takes several shadows.
[*] [b]Conic gradients[/b] (GC), clockwise from a start angle, with a hard edge at the seam.
[*] [b]Concave clips.[/b] Any polygon or path can clip; text is masked to rounded and concave outlines with a stencil.
[*] [b]IMG[/b]: a picture from a URL, with object-fit, corner radii and opacity, drawn in scene order.
[*] Groups take CSS [b]filters[/b] (bri con sat hue gray sep inv), a gradient [b]mask[/b], and a CSS [b]matrix[/b] m.
[*] Fixed: text applied fo and group opacity twice, so a label at 50% drew at 25%.
[*] Fixed: shadows ignored group opacity and fo, so a faded shape kept a full-strength shadow.
[*] Text takes a gradient fill (f=@name) and first-line styling (fl). IMG takes uv, a crop of the picture.
[*] T align=justified. fat and sat sample a gradient in the text scene format. SC so and sov set the scroll offset once per version.
[/list]
[b]0.10.5[/b]
[list]
[*] Fixed: a console that ran out of room once kept its magenta border for ever, because running out was filed with the scene's parse-time faults, which are never cleared. It is a fault of the size you are standing at, so it now clears when it stops being true.
[*] [b]Feathered shapes cost a third less, and shadows nearly half[/b], with no change to how either looks. Both were emitting a contour twice: a feather ring wrote its inner edge at the same positions and colours the fill had just written, and each ring of a blurred shadow rewrote the contour the previous ring had already laid down. They index the shared edge now. Measured on 400 feathered circles: 60 vertices each became 40.
[/list]
[b]0.10.4[/b]
[list]
[*] [b]Running out of room says so.[/b] Going over the ceiling used to drop geometry silently -- something on the console was simply missing, with nothing in the log and nothing on screen to say which part. It now raises a scene problem naming what was refused, so it gets the magenta border and a line in vector_stats like every other fault.
[*] Fixed: vector_stats reported a nonsense rebuild rate unless Diagnostics was switched on, because it divided by the reporting interval whether or not a report had happened.
[*] The 60,000-vertex ceiling stays. It is UGUI's own limit, not a setting -- raising it is only possible by splitting a surface across several meshes.
[/list]
[b]0.10.3[/b]
[list]
[*] [b]Radial gradients cost about half the mesh they used to[/b], and a small many-stop one far less. Band count now respects what the screen can resolve -- a thirteen-stop ramp on a six-pixel shape was drawing ninety-six bands of detail finer than a pixel -- and rings near the centre carry fewer points, since a ring at a tenth of the radius has a tenth of the circumference. This matters because a surface has a hard vertex ceiling that fails by dropping geometry silently.
[/list]
[b]0.10.2[/b]
[list]
[*] [b]Shadows work on every closed shape[/b], not only rectangles and ellipses. A filled polygon, spline or closed path parsed sh and silently drew nothing; the documentation claimed otherwise and was wrong.
[*] [b]Text takes a shadow too[/b], through the font's own underlay, since a glyph has no outline for the geometric shadow to trace. One shadow per label, and a request larger than the font's SDF padding allows is scaled down as a whole and reported rather than quietly shrunk.
[/list]
[b]0.10.1[/b]
[list]
[*] Text nodes take wrap=1 for multi-line paragraphs and lh for line height, as a CSS-style multiple of the font size. Single line stays the default, since a readout that silently becomes two lines shifts everything the scene placed around it.
[*] Backslash escapes inside quoted values in the text scene format, so a string can contain the quote that delimits it.
[/list]
[b]0.10.0[/b]
[list]
[*] [b]A whole list is one node.[/b] Text and colours can be bound to an array slot -- text="$rows[i]" and f="$tints[i]" inside a repeat -- so thirty rows are one node and one array instead of thirty nodes. A clickable node in a repeat now reports which instance was hit.
[*] [b]Text can format a number itself[/b], with fmt="%.1f" and unit=" kPa", so the chip sends the number it already had and does no string work at all.
[*] [b]keep=1 on the data element[/b] makes a payload a patch: names it does not mention hold their values. Without it every string on screen had to be resent every tick or it vanished.
[*] Groups in the text scene format can carry paint defaults directly, since that format has no map syntax for style.
[*] vector_stats reports the addon version on its first line.
[*] Fixed: a label with no font of its own could inherit the face of whichever label used that pooled object before it. Fixed: string and colour arrays were parsed out of the payload and then dropped before the renderer saw them.
[/list]
[b]0.9.1[/b]
[list]
[*] Text now fades with its group. A T node takes fo, and the enclosing group's o reaches it -- so a row's text fades at the edge of a scroll container along with the artwork around it, which it previously did not.
[*] Corrected the documentation's instruction-budget claims after a real console was ported and measured. Writing a scene as text is free when it is a literal and costs about what tables cost when it is generated; it is worth choosing for clipping, scrolling, click regions and live text, not for the budget.
[/list]
[b]0.9.0[/b]
[list]
[*] [b]Scroll containers.[/b] SC is a box that clips to itself and slides its children inside it: wheel over it or drag it and a list longer than the console moves. The scroll position stays on the client, so a wheel notch costs one mesh rebuild and nothing else -- no tick, no network message, and none of the chip's instruction budget.
[*] Inside a container, the sy and vh expression variables report that container, so a pinned header, an edge fade or a scrollbar thumb is written exactly as it is for a scroll view.
[/list]
[b]0.8.0[/b]
[list]
[*] [b]Scenes report their own faults.[/b] An unknown op or attribute, a malformed expression, a data name with no value, a missing gradient or clip — each is reported once with the node it came from, and the surface shows a magenta border rather than looking switched off. A colour bound to a name the payload never supplied now draws magenta instead of white, so an unresolved binding looks like a fault rather than a design decision.
[*] [b]vector_stats[/b], an MCP tool for StationeersLua: every live surface's node and shape counts, rebuild rate, tessellation and upload cost, on-screen size, and any problems or unresolved names. Bound by reflection, so nothing changes if StationeersLua is not installed.
[/list]
[b]0.7.0[/b]
[list]
[*] [b]Clickable nodes.[/b] A node marked click=1 becomes a hit region, and the click arrives at the vector element's own on_click with the node id as its value. A list of tappable rows no longer needs a transparent button element over every one of them.
[/list]
[b]0.6.0[/b]
[list]
[*] [b]Symbols.[/b] Declare a subtree once with SYM and stamp it with USE, passing parameters — a row, an LED, a duct segment written once and placed twenty times. Substitution happens at parse time, so an instance costs exactly what writing the nodes out would have.
[*] [b]Inherited defaults.[/b] A style map on a group or the scene root supplies defaults for everything under it that does not set the key itself.
[*] [b]Per-corner radii[/b] on rectangles: rx = { tl, tr, br, bl }, CSS order, and a zero corner is a sharp point.
[/list]
[b]0.5.0[/b]
[list]
[*] New [b]T[/b] node: text inside the scene, drawn with TextMeshPro so registered fonts and rich text work. It moves, scales and scrolls with its group like any other node, updates from the data payload with text = "$name", and supports ellipsis and shrink-to-fit. Text was previously label elements laid over the scene, which cost about 300 Lua instructions each, had to be re-declared to change a word, and could not move with the artwork.
[*] Strings are now data values, so a data payload can carry text as well as numbers, arrays and colours.
[*] Known limit: text clips to an axis-aligned rectangle only. A rounded or rotated clip does not cut it until stencil clipping lands.
[/list]
[b]0.4.1[/b]
[list]
[*] Fixed: a fill written as [b]f = "$name"[/b] never worked. The colour was parsed out of the data payload correctly and then dropped before it reached the renderer, so every data-bound fill fell back to white. Colours now arrive like numbers and arrays do.
[*] New [b]nodes[/b] prop on the data element: patch any node that carries an id, without resending the scene — nodes = { hv_bar = { w = 42, f = "#E23D3D" } }.
[/list]

[b]0.4.0[/b]
[list]
[*] A scene can now be written as one string in a [b]src[/b] prop instead of nested tables — one node per line, braces for children. A static layout written this way is a literal, already in the compiled script, so it costs nothing to build; a generated one costs about what the tables cost. Same ops, keys and expressions as the table form, and it goes through the same parser so the two cannot drift.
[/list]

[b]0.3.0[/b]
[list]
[*] New [b]sh[/b] attribute: CSS box-shadow, several composing in order — { dx, dy, blur, spread, colour }. Drawn as real geometry from the closed form for a blurred edge, so it needs no offscreen pass and no shader. This is the thing feathering could not do: feather ramps outward from a solid edge while a blur softens both sides of it, which is why a feathered knob reads as a ring instead of a shadow.
[*] New expression variables [b]sy[/b] and [b]vh[/b] — the scroll offset and viewport height of an enclosing scroll view, in scene units. A vector element inside a scroll view moves with the content; adding sy cancels that out, so a header, fade or rule can stay pinned to the viewport while the content scrolls underneath. Both read 0 when there is no scroll view, so the same scene works either way, and a scene that uses them redraws as you scroll even if it never mentions time.
[/list]

[b]0.2.1[/b]
[list]
[*] The eight worked examples, the authoring guide and the full reference now install with the mod, in the mod's own folder. Previously only the DLL was copied, so there was nothing to read or paste.
[*] Fixed: two consoles running the same script fought over one scene registration. Every console names its surface "main", so both hashed to the same key and one console's data reached the other's artwork — seen as clipping breaking on whichever console was started second, and swapping over on restart. Scenes are now keyed per board.
[/list]

[b]0.2.0[/b]
[list]
[*] Tessellation moved to a worker thread. A console with 800 animated shapes at 30 Hz now costs about 0.4% of a frame on the main thread — switching one on is no longer measurably different from switching it off.
[*] New [b]fo2[/b] on sampled bands: an opacity ramp along each column, from the sampled edge to the far one. This is how you fade from a surface that moves; a gradient cannot, because it is anchored to the bounding box rather than the wave.
[*] Gradients gained [b]units = "bbox"[/b] — a ramp that spans whatever shape references it, and follows that shape when it moves or resizes.
[*] Curve level of detail: sampled bands and lines are walked with a longer step when drawn small. Nothing is dropped, the same curve is simply traced with fewer segments.
[*] Circles now tessellate by on-screen radius instead of a flat 32 segments, so particles are cheap and large dials are smooth.
[*] Shapes that resolve to zero opacity are skipped rather than tessellated, which makes opacity usable as a visibility switch.
[*] Clipping no longer allocates per boundary edge, and gained bounds fast paths. Much faster on clipped scenes.
[*] Feathering survives a clip and can ramp one edge only ([b]fea_edge[/b]).
[*] Clip paths are interpreted in scene coordinates, so a clip keeps clipping the same area however the groups under it are transformed.
[*] A scene that draws nothing now shows a magenta hatched border instead of an empty console, with the reason in the log.
[/list]

[h2]Source[/h2]
[url=https://github.com/Gruffuss/scripted-screens-vector]github.com/Gruffuss/scripted-screens-vector[/url] — MIT licensed. Issues and pull requests welcome.

[h2]Requirements[/h2]
[list]
[*] [url=https://steamcommunity.com/sharedfiles/filedetails/?id=3666779631]ScriptedScreens[/url] — [b]required[/b]; load before this mod
[*] [url=https://github.com/StationeersLaunchPad/StationeersLaunchPad]StationeersLaunchPad[/url]
[*] [url=https://github.com/BepInEx/BepInEx]BepInEx 5.x[/url]
[/list]
	</Description>
	<WorkshopHandle>3787517570</WorkshopHandle>
	<Tags>
		<Tag>Mod</Tag>
		<Tag>BepInEx</Tag>
		<Tag>StationeersLaunchPad</Tag>
	</Tags>
	<ModID>gruffuss.stationeers.scriptedscreens.vector</ModID>
	<Dependencies>
		<Mod>
			<Id>3666779631</Id>
		</Mod>
	</Dependencies>
</ModMetadata>
